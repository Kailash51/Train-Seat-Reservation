import { Coach } from "../types";

export const findAvailableSeats = (
  coach: Coach,
  numSeats: number
): number[] => {
  // Implementation
  return [];
};

export const bookSeats = (coach: Coach, seatIds: number[]): Coach => {
  // Implementation
  return coach;
};

// Remove the updateCoach function if it's not used elsewhere in the application
